import './App.css'

function App() {
  return (
    <main className="app">
      <section className="card" aria-labelledby="welcome-title">
        <p className="eyebrow">React + Vite</p>
        <h1 id="welcome-title">Hello World!</h1>
        <p className="message">
          The app is ready for the web today and a Capacitor shell tomorrow.
        </p>
      </section>
    </main>
  )
}

export default App
